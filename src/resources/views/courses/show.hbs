<div class="mt-4">
    <div class="row">
        <div class="col-lg-3">
            <button>Enroll</button>
        </div>

        <div class="col-lg-9">
            <h2>{{course.name}}</h2>
            <img src="{{course.image}}" alt="">
            <p class="mt-4">{{course.description}}</p>
        </div>
    </div>
</div>
